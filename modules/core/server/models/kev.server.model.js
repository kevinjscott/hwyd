'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
  Schema = mongoose.Schema;

/**
 * Kev Schema
 */
var KevSchema = new Schema({
  // Kev model fields
  // ...
});

mongoose.model('Kev', KevSchema);
